# --- Core ---
# 6.0.0 shipped with numerous CVEs (see pip-audit); stay on the patched 6.0.x line.
Django>=6.0.6,<6.1
djangorestframework==3.16.1
djangorestframework-simplejwt==5.5.1
drf-yasg==1.21.11

# --- Required for base functionality (CORS, images, Telegram, env) ---
django-cors-headers
Pillow
requests
python-decouple

# --- Production ---
gunicorn==26.0.0
psycopg[binary]==3.3.4
dj-database-url==3.1.2
whitenoise==6.12.0
# S3-compatible object storage for media (switches on via env creds; see settings)
django-storages==1.14.6
boto3==1.43.26
